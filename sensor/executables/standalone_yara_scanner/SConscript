Import( 'env' )
Import( 'compmap' )
import profiles

profiles.make_rpal_master( env )
profiles.Program(
        'standalone_yara_scanner',
        profiles.RpalModule()
        ).Target(
            env,
            'main.c',
            compmap,
            'processLib', 
            'Os',
            'libyara',
            )

# EOF
